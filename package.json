{
    "name": "github-search-wali-faizy",
    "version": "1.0.0",
    "description": "Assignment",
    "main": "index.js",
    "author": "Wali Faizy",
    "license": "MIT",
    "scripts": {
        "dev": "next",
        "build": "next build",
        "start": "next start",
        "export": "next export",
        "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && git add out/ && git commit -m \"Deploy Next.js to gh-pages\" && git subtree push --prefix out origin gh-pages && git push origin master"
    },
    "dependencies": {
        "next": "^9.2.1",
        "react": "^16.12.0",
        "react-dom": "^16.12.0"
    },
    "devDependencies": {
        "@types/node": "^9.4.6",
        "@types/react": "^16.0.38",
        "typescript": "^3.7.5"
    }
}
