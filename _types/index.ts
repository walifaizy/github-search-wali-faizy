export type TUserData = {
    avatar_url: string;
    email: string;
    isLoading: boolean;
    name: string;
    html_url: string;
    bio: string;
};

export type TRepo = {
    name: string;
    url: string;
    html_url: string;
};
